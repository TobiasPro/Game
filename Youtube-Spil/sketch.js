function setup() {
    createCanvas(windowWidth, windowHeight);
}

function draw() {
    background(0);
}

function ship() {
    this.pos = createVector(width/2, height/2)

    this.render = function() {

        
    }
}